<UserControl x:Class="teste_tecnico.Views.ProdutosView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:teste_tecnico.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>

        <!-- Filtros -->
        <WrapPanel Grid.Row="0" Grid.Column="0" Orientation="Horizontal" Margin="0,0,0,10">
            <TextBlock Text="Nome:" VerticalAlignment="Center" Margin="5"/>
            <TextBox Text="{Binding FiltroNome, UpdateSourceTrigger=PropertyChanged}" Width="150" Margin="5"/>
            <TextBlock Text="Código:" VerticalAlignment="Center" Margin="5"/>
            <TextBox Text="{Binding FiltroCodigo, UpdateSourceTrigger=PropertyChanged}" Width="100" Margin="5"/>
            <TextBlock Text="Valor de:" VerticalAlignment="Center" Margin="5"/>
            <TextBox Text="{Binding FiltroValorInicial, UpdateSourceTrigger=PropertyChanged, StringFormat=C2}" Width="80" Margin="5"/>
            <TextBlock Text="até:" VerticalAlignment="Center" Margin="5"/>
            <TextBox Text="{Binding FiltroValorFinal, UpdateSourceTrigger=PropertyChanged, StringFormat=C2}" Width="80" Margin="5"/>
        </WrapPanel>

        <!-- Grid de Produtos -->
        <DataGrid Grid.Row="1" Grid.Column="0" ItemsSource="{Binding ProdutosView}" 
                  SelectedItem="{Binding SelectedProduto}"
                  AutoGenerateColumns="False" CanUserAddRows="False" IsSynchronizedWithCurrentItem="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding Id}" IsReadOnly="True"/>
                <DataGridTextColumn Header="Nome" Binding="{Binding Nome, UpdateSourceTrigger=PropertyChanged}" Width="*"/>
                <DataGridTextColumn Header="Código" Binding="{Binding Codigo, UpdateSourceTrigger=PropertyChanged}"/>
                <DataGridTextColumn Header="Valor" Binding="{Binding Valor, UpdateSourceTrigger=PropertyChanged, StringFormat=C2}"/>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Botões de Ação -->
        <StackPanel Grid.Row="1" Grid.Column="1" Margin="10,0,0,0">
            <Button Content="Incluir" Command="{Binding AddCommand}" Margin="5"/>
            <Button Content="Salvar" Command="{Binding SaveCommand}" Margin="5"/>
            <Button Content="Excluir" Command="{Binding DeleteCommand}" Margin="5"/>
        </StackPanel>
    </Grid>
</UserControl>